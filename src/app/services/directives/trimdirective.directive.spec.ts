import { TrimdirectiveDirective } from './trimdirective.directive';

describe('TrimdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
